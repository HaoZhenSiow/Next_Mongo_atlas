import ipify from 'ipify2'
import axios from "axios"
axios.defaults.validateStatus = false

export default async function tracker() {
  // const time = Date.now()
  // ipify.ipv4().then(ipv4 => console.log(ipv4, location.pathname, time)).catch(err => console.log(err));
  // console.log(location.pathname, time);
  // axios.get('http://localhost:3000/api/tracker/')
}
1. copy the whole admin folder to the app folder
2. set .env.local
3. npm i styled-components js-cookie axios bower jose mongoose dotenv easy-peasy bcrypt react-date-range
4. import NavigationEvents and place it at the very top in RootLayout, if an app have force redirection, place it after the code that force redirect, example Navbar
5. create eventsTracker.js to define the events going to track other than navigation events